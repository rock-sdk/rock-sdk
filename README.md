# rock-sdk


## Building (Visual Studio 2022)
- Right click root (rock-sdk/)
- Open in terminal
- Run ```cmake -B build```
- Run ```cmake --build build --config Release``` for release or ```cmake --build build --config Debug``` for debug
- The DLL should be in rock-sdk/build/loader/dll/Release/rockloader.dll
